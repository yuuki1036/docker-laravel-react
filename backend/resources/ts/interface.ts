export type TableData = {
    id: number;
    name: string;
    content: string;
    editBtn: JSX.Element;
    deleteBtn: JSX.Element;
};

export type FormData = {
    name: string;
    content: string;
};
